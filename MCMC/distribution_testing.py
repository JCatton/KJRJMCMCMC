from mcmc import MCMC, Statistics
from scipy.stats import multivariate_normal
import numpy as np

def main():
    mean = np.zeros(1, dtype=np.float64)
    covariance = np.diag(np.ones(1, dtype=np.float64))



if __name__ == '__main__':
    main()